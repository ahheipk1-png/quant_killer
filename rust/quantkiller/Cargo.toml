[package]
name = "quantkiller"
version = "0.1.0"
edition = "2021"
description = "Free, open, verified derivatives pricing -- Rust reference-parity engine"

[lib]
name = "quantkiller"
crate-type = ["rlib"]

[dependencies]

[dev-dependencies]
serde_json = "1"
